import { RevokeClient } from '@/components/RevokeClient';
import { SiteShell } from '@/components/SiteShell';

export default function RevokePage() {
  return (
    <SiteShell>
      <RevokeClient />
    </SiteShell>
  );
}
